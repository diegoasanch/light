/** @type {import('next').NextConfig} */
const nextConfig = {
  // The viewer is fully static — keeps the door open for exporting into a
  // portfolio site later (`next build` emits a static bundle with `output: 'export'`).
  reactStrictMode: true,
};

export default nextConfig;
